# Voice Agent — Raspberry Pi / macOS / Windows dependencies
# Install: pip install -r voice/agent/requirements.txt
#
# macOS: requires PortAudio first → brew install portaudio
# Linux/RPi: sudo apt install portaudio19-dev python3-pyaudio

# Wake word
openwakeword

# Audio I/O
pyaudio
numpy
webrtcvad

# HTTP client (async)
httpx

# Sound generation (one-time, can skip if pre-generating on dev machine)
scipy
