# ── Core server ───────────────────────────────────────────────────────
fastapi
uvicorn[standard]
python-multipart
bleak
websockets

# ── Voice Service (server-side) ───────────────────────────────────────
# Windows/Linux CUDA:
faster-whisper
# macOS Apple Silicon — uncomment and comment out faster-whisper above:
# mlx-whisper
httpx
edge-tts
librosa
soundfile

# ── Voice Agent (Raspberry Pi / local mic) ────────────────────────────
# Install separately: pip install -r voice/agent/requirements.txt
# (requires portaudio: brew install portaudio / apt install portaudio19-dev)
openwakeword
